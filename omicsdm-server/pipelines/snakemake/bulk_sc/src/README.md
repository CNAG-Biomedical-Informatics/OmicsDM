The files in here are symbolic links to the actual source files.
So modifications in here modify the actual source files.

Thus, before merging these changes (with the main branch) 
copy the modified files to an extra folder so the scripts 
inside the folders inside 
pipelines/snakemake are not overwritten !

