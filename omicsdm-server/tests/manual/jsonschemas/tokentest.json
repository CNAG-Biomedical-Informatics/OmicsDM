{
  "type": "object",
  "required": ["user", "groups", "project"],
  "properties": {
    "user": {
      "type": "string"
    },
    "groups": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["group1", "group2"]
      }
    },
    "project": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["no_project"]
      }
    }
  }
}
